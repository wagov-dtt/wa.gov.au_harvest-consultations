apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mariadb
  labels:
    app: mariadb
spec:
  serviceName: mariadb
  selector:
    matchLabels:
      app: mariadb
  template:
    metadata:
      labels:
        app: mariadb
    spec:
      containers:
        - name: mariadb
          image: mariadb:11
          ports:
            - containerPort: 3306
              name: mysql
          env:
            - name: MARIADB_ROOT_PASSWORD
              value: harvest
            - name: MARIADB_DATABASE
              value: harvest
          readinessProbe:
            exec:
              command: ["mariadb-admin", "-uroot", "-pharvest", "ping"]
            initialDelaySeconds: 10
            periodSeconds: 5
