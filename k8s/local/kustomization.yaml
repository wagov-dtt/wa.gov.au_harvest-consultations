namespace: harvest
resources:
  - namespace.yaml
  - ../base/mariadb.yaml
  - ../base/cronjob.yaml
images:
  - name: ghcr.io/wagov-dtt/harvest-consultations
    newName: harvest-consultations
    newTag: test
secretGenerator:
  - name: harvest-env
    envs:
      - ../../.k8s.env
    literals:
      - DB_HOST=mariadb
generatorOptions:
  disableNameSuffixHash: true
